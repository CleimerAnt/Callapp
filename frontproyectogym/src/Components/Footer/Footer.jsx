import styles from '../Footer/Footer.module.css'

export default function Footer(){
    return (
        <footer className={`d-flex align-items-center justify-content-center ${styles.footer}`}>
            <p className="h1">CLEIMER ANT.</p>
        </footer>
    );
}
